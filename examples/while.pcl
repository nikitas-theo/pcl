program aa;
    var i, seed : integer;
    begin
        i := 0;
        seed := 0;
        while i < 16 do
        begin
            seed := seed + i ;
            i := i + 1
        end;
        writeInteger(seed)
    end.
