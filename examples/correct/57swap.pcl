program sw;

   procedure swap (var a, b : integer);
      var t : integer;
   begin
      t := a;
      a := b;
      b := t
   end;

   var x, y : integer;

begin
   x := 2;
   y := 4;
   swap(x, y);
   writeInteger(x);
   writeInteger(y);
   writeString("\n")
end.
